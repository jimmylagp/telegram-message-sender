import styled from 'styled-components'

const Title = styled.h1`
  color: #2d2e30;
  font-size: 30px;
  line-height: ${36 / 30};
  font-weight: 300;
  margin-top: 0.5em;
  margin-bottom: 1em;
`
export default Title
